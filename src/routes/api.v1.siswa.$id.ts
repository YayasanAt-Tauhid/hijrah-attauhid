import {createFileRoute} from '@tanstack/react-router'; import {handleDetail} from '@/server/integrationApi'; export const Route=createFileRoute('/api/v1/siswa/$id')({server:{handlers:{GET:({request,params})=>handleDetail(request,'siswa',params.id)}}});